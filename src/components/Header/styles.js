import styled from "styled-components";

export const Container = styled.header`
  display: flex;

  h1 {
    margin: 3rem auto 4rem;
    color: var(--main-red);
    font-size: 2.5rem;
    font-weight: 800;
  }
`;